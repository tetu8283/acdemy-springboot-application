<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.springbootapplication.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.spring.springbootapplication.entity.Category">
        <id column="category_id" property="categoryId" />
        <result column="category_name" property="categoryName" />
        <result column="category_type" property="categoryType" />
    </resultMap>

    <!-- カテゴリを挿入 -->
    <insert id="insertCategory" parameterType="com.spring.springbootapplication.entity.Category">
        INSERT INTO Category(category_id, category_type, category_name) VALUES(#{category_id}, #{category_type}, #{category_name})
    </insert>


    <select id="findAll" resultType="com.spring.springbootapplication.entity.Category">
        SELECT category_id, category_type, category_name FROM Category
    </select>

</mapper>
